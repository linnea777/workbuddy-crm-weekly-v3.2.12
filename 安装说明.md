# 安装与升级

建议新建3.2.12目录并保留旧包。完整导入`.codebuddy/skills`内六个技能，或分别上传每个技能完整目录；保留各自的aicrm-skill.json、runtime、references和assets，不混装旧版本。把SystemPrompt.md配置为CRM周报Agent提示词。模型由WorkBuddy宿主选择，项目代码不设置外部模型API。

安装requirements.txt。通过AICRM_PYTHON指定已安装pandas/openpyxl的Python 3.10+解释器；分散安装时可用--skill-map或AICRM_SKILL_MAP指定技能映射。

按prepare → investigate → 宿主模型按writing_context索引逐主题写草稿 → finalize --draft运行。完整命令见crm-weekly-report/SKILL.md。旧版本计划和洞察不能直接混用，新目录中重新prepare。改变源文件或报告周时重新prepare；只改正文或模板可重新finalize。新版增加行动交付字段，旧洞察需先按action-delivery.md转换，不能直接宣称兼容。使用同一运行的原始指标与证据重做写作时，可执行--phase writing生成索引及草稿模板。

活动调查按需补充日级数据。已有同一输入的指标产物时只补日级细节，无需重算全部周报指标；相关过滤条件见crm-data-investigation/references/contract.md及周报活动政策。历史活动、假期核查应附来源，资料不足不得声明已确认无活动。

可通过--coverage-json提供自然周和区域的来源完整性。未提供时，系统保留覆盖限制。只向业务交付三页Excel，不默认交付中间JSON或审计包；内部运行目录含审计资料，按原有访问范围保管。

项目不会自动联网、发邮件或发布报告。timing_*.json仅记录本地程序阶段，不包含宿主模型思考和人工检查时间。

本版保留3.2.9认可的建议风格与四个完整few-shot。phase_history.jsonl记录各次成功及失败；各轮调查计时分别保存，仍不代表宿主模型思考时间。
